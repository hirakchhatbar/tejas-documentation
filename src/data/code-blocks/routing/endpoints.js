const createEndpoint = `// This registers a route at "/hello and "/another-hello" that you can send requests to.
target.register("/hello", (ammo) => {
  // Your code here
});

target.register("/another-hello", (ammo) => {
  // Your code here
})`

export { createEndpoint }
